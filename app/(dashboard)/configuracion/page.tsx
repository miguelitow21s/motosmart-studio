import { ConfiguracionClient } from "@/features/configuracion/components/ConfiguracionClient"

export const metadata = { title: "Configuración — MotoSmart Studio" }

export default function ConfiguracionPage() {
  return <ConfiguracionClient />
}
